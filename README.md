# Meu Sistema Dashboard

Este é um projeto de dashboard administrativo construído com Angular e Angular Material.

## 🚀 Como rodar o projeto

1.  **Instalar dependências:**
    ```bash
    npm install
    ```

2.  **Rodar o servidor de desenvolvimento:**
    ```bash
    ng serve
    ```

    O projeto estará disponível em `http://localhost:4200`.

## 🛠️ Tecnologias Utilizadas

* Angular
* Angular Material
* CSS (com Dark Mode)